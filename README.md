# Base Gold Rush – Poker / Table Game Coordinator

Simple Node + WebSocket server that coordinates multiplayer tables
(poker tournaments, blackjack, etc.) for Base Gold Rush.

## Quickstart

```bash
git clone <your-repo-url> bgld-poker-coordinator
cd bgld-poker-coordinator
cp .env.example .env   # edit PORT / ALLOWED_ORIGIN
npm install
npm run dev
